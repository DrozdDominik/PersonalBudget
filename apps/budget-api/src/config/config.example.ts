import { Config } from "../types";

export const config: Config = {
    db: {
        host: '',
        username: '',
        password: '',
        database: ''
    },
    jwt: {
        jwtSecret: '',
        expirationTime: 0
    },
}